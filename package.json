{
  "name": "dsh-next-app",
  "private": true,
  "description": "pnpm workspace root for dsh-next-app: private, never published (ADR-0002)",
  "scripts": {
    "build": "pnpm --filter @scope/dsh-next-app run build",
    "test": "pnpm -r --if-present run test",
    "lint": "pnpm -r --if-present run lint",
    "format": "oxfmt ."
  },
  "devDependencies": {
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "oxlint-tsgolint": "catalog:"
  },
  "engines": {
    "node": ">=24"
  },
  "packageManager": "pnpm@11.22.0"
}
