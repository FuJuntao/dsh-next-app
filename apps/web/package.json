{
  "name": "web",
  "version": "0.0.0",
  "private": true,
  "description": "Next.js App Router frontend for dsh-next-app (ADR-0001); built output ships inside the dsh-next-app bundle, not as a standalone service",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "lint": "oxlint .",
    "format": "oxfmt ."
  },
  "dependencies": {
    "next": "catalog:",
    "react": "catalog:",
    "react-dom": "catalog:"
  },
  "devDependencies": {
    "@tsconfig/next": "catalog:",
    "@tsconfig/strictest": "catalog:",
    "@types/node": "catalog:",
    "@types/react": "catalog:",
    "@types/react-dom": "catalog:",
    "dsh-api": "workspace:*",
    "typescript": "catalog:"
  }
}
